// const favoritesRouter = require("express").Router();
// const { postFav, deleteFav } = require("../controllers/handleFavorites");

// favoritesRouter.post("/", postFav);
// favoritesRouter.delete("/", deleteFav);

// module.exports = favoritesRouter;
